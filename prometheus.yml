global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'net-core-app'
    scheme: https
    metrics_path: '/metrics'
    tls_config:
      insecure_skip_verify: true
    static_configs:
      - targets: ['finanzas.api:8081']